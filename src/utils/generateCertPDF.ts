// generateCertPDF.ts
// Day 10에서 구현 예정: CreatorData → 품목제조보고서 PDF 생성

import type { CreatorData } from '../pages/creator/types';

// TODO Day 10: nutritionExempted === true 이면
// "영양성분 표시 면제 적용 (식품등의 표시기준 제5조)" 명시

export function generateCertPDF(_data: CreatorData): void {
  // 구현 예정
}
